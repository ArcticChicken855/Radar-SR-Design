/*
 * This is a source file without code.
 *
 * Some native build systems (such as Xcode) may ignore targets that have only object files,
 * so this file needs to be added to any such static or shared library.
 *
 */
